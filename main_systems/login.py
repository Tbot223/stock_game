#login system
from main_systems import db

def login()
    db.db_check()
    name = input
            for i in SaveFilse:
            data =  [
                        {
                            "name" : "Player",
                            "money" : 100000
                        }

                    ]
            if not os.path.isdir("./db/AutoSave/" + i):
                with open("./db/AutoSave/" + i, "w") as f:
                    json.dump(data,f)
    